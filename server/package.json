{
  "name": "server",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.7.7",
    "kysely": "^0.28.1",
    "kysely-d1": "^0.4.0"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}